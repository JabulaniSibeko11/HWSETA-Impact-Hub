using Microsoft.AspNetCore.Identity;

namespace HWSETA_Impact_Hub.Infrastructure.Identity
{
    public sealed class ApplicationUser : IdentityUser
    {
        // Extend later if needed (e.g., full name)
    }
}
